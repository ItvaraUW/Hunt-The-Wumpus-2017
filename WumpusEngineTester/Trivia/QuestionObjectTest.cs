using System;
using Microsoft.VisualStudio.TestTools.UnitTesting;
using WumpusEngine;
using System.IO;
using WumpusEngineTester;

namespace WumpusEngineTester
{
    [TestClass]
    public class QuestionObjectTest
    {
        [TestMethod]
        public void TestMethod1()
        {
            //Random rand = new NotRandom(0, 3);
            //StreamReader reader = new StreamReader("Trivia\\questions.txt");
            //Question q = new Question(reader, rand);

            //q.LoadNewQuestion();
            //Assert.AreEqual("What is the average airspeed velocity of an unladen swallow?", q.GetQuestion());
            //string[] choices = q.GetChoices();
            //Assert.AreEqual("100 m/s", choices[0]);
            //Assert.AreEqual("African or European?", choices[1]);
            //Assert.AreEqual("4", choices[2]);

            //q.LoadNewQuestion();
            //Assert.AreEqual("Which animal sleeps up to 22 hours a day?", q.GetQuestion());
            //string[] choices2 = q.GetChoices();
            //Assert.AreEqual("Koala", choices2[0]);
            //Assert.AreEqual("Sloth", choices2[1]);
            //Assert.AreEqual("Rabbit", choices2[2]);
        }
    }
}
